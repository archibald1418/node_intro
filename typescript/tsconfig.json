{
    "compilerOptions": {
        "target": "ES5", // flavour of JS
        "watch": true, // recompiles if index.ts is changed
        "lib": [
            // includes typing from these namespaces
            "DOM",
            "ES2017"
        ]
    }
}